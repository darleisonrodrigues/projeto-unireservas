# Datasource Prometheus provisionado automaticamente
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus            # uid fixo referenciado pelos dashboards
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
      httpMethod: POST
