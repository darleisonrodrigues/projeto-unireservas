server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Necessário para que o roteamento do React funcione (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }

}
